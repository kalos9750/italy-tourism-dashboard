# RapidAPI key used by the /api/* serverless functions (Booking.com proxy).
# Server-side only — NOT exposed to the browser.
# In local dev: copy to .env.local and run `npm run dev:vercel` (requires Vercel CLI).
# In production: set this as an Environment Variable in the Vercel dashboard.
RAPIDAPI_KEY=your_rapidapi_key_here
